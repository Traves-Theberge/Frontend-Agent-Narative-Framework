/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add any existing configurations here
  // Removed the unused custom webpack configuration previously added for Pyodide
};

export default nextConfig; 